结合多智能体协作（Multi-Agent）和“人机协同（Human-in-the-loop）”的设计理念，我为你梳理了一套针对该绘图系统的 Agent 工作流架构：1. 核心工作流设计：三步走架构我们可以将这个过程拆分为三个协同工作的 Agent 模块，或者一个包含三个节点的标准工作流（例如基于 CoT 思维链的单 Agent）：节点 A：意图解析 Agent (Intent Analyzer)输入： 用户的原始自然语言（例如：“帮我画一张城市更新效果图，带点赛博朋克风，傍晚的”）。任务： 从非结构化文本中提取结构化标签。系统提示词节选：“你是一个意图分析引擎。请阅读用户的输入，并输出 JSON 格式的解析结果，包含四个维度：1. 核心主体 (Subject)；2. 时间/光照 (Time/Lighting)；3. 氛围/风格 (Mood/Style)；4. 视角大小 (Scale)。”节点 B：参数映射与增强 Agent (Parameter Mapping & RAG)输入： 节点 A 输出的 JSON 数据。任务： 充当“摄影指导”和“美术指导”。根据解析出的维度，从你的专业参数知识库中自动检索并匹配最合适的摄影术语。逻辑示例：如果 Scale 是“城市更新/宏大场景” $\rightarrow$ 自动挂载：14mm ultra-wide angle, f/11 aperture, bird's-eye view。如果 Time/Lighting 是“傍晚” $\rightarrow$ 自动挂载：Blue Hour, glowing neon lights, cinematic lighting。如果 Mood/Style 是“赛博朋克” $\rightarrow$ 自动挂载：Cinestill 800T film, red halation, high contrast。节点 C：提示词融合与输出 Agent (Prompt Synthesizer)输入： 用户原始需求 + 节点 B 匹配的专业参数。任务： 将所有元素融合成一段符合 AI 绘图模型（如 Stable Diffusion、Midjourney 或 Gemini 图像模型）语法的英文提示词，并优化权重排布。2. 人机协同体验设计 (Human-in-the-loop)虽然 Agent 在后台完成了极其复杂的匹配工作，但在实际的产品交互（UI/UX）上，建议保留一个**“确认与微调”**的环节，而不是直接黑盒生成图像。这能让专业用户（如设计师、规划师）保持对生成结果的掌控感。推荐的交互流程：用户在输入框打字：“一个雨后的极简风格美术馆”。点击“智能增强”按钮。Agent 工作流在 1-2 秒内跑完上述 A-B-C 节点。UI 界面不直接出图，而是弹出一个“参数控制台”（Spotlight 样式的悬浮窗或侧边栏），展示 Agent 为他自动选好的标签（以 Tag 形式呈现），例如：镜头：[24mm Tilt-Shift] [f/8]光照：[Overcast] [Soft Diffused Light]材质：[Concrete texture] [Puddle reflections]用户可以审阅这些 Tag，点击“X”删掉不想要的，或者手动添加新的 Tag。确认无误后，点击“生成图像”。3. 给“意图分析 Agent”的底层 Prompt 示例如果你打算用单个强大的 LLM 来直接完成这个工作流，可以使用以下提示词模板：Plaintext你是一个专业的 AI 绘图提示词构建 Agent。
你的任务是：读取用户的简短输入，分析其意图，并自动匹配顶级的摄影参数和艺术风格，最终输出一段结构化的英文 Prompt。

【参数匹配规则库】
1. 建筑/城市景观意图：强制使用 "24mm tilt-shift lens", "f/11 aperture", "architectural photography"。
2. 人物/特写意图：强制使用 "85mm lens", "f/1.4 aperture", "shallow depth of field"。
3. 电影感/情绪意图：匹配 "shot on ARRI Alexa 65", "cinematic lighting", "volumetric light"。
4. 真实感/写实意图：匹配 "Kodak Portra 400" 或 "Fujifilm Velvia 50", "8k resolution", "hyper-realistic"。

【工作流要求】
1. 分析用户输入的主体和场景。
2. 从上述规则库中选取最匹配的 1-2 组参数。
3. 使用英文输出最终的绘图 Prompt，遵循格式：
   [核心主体细节], [环境背景], [光影与天气], [匹配的摄影镜头参数], [匹配的胶片/渲染引擎参数], [画质修饰词]

用户输入：{user_input}
请先简要用中文输出你的【分析过程】，然后输出最终的【英文 Prompt】。